<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FoodOrdering" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary</item>
    </style>

    <!-- Element Styling -->
    <style name="EditText">
        <item name="android:background">@drawable/background_outline_rounded</item>
        <item name="android:padding">10dp</item>
    </style>

    <style name="activeItem">
        <item name="itemIconTint">@color/white</item>
        <item name="itemTextColor">@color/white</item>
    </style>

    <style name="Theme.FoodOrdering" parent="Base.Theme.FoodOrdering" />
</resources>